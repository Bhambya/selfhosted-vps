name: Continuous Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  deploy:
    name: Deploy Ansible Playbook
    runs-on: ubuntu-latest
    concurrency:
      group: host_management

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Ansible and dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ansible
          pip install bitwarden-sdk
          ansible-galaxy collection install -r ./ansible/requirements.yml
          ansible-galaxy role install -r ./ansible/requirements.yml

      - name: ⬇️ Get Secrets from Bitwarden
        uses: bitwarden/sm-action@v2
        with:
          access_token: ${{ secrets.BWS_ACCESS_TOKEN }}
          secrets: |
            38c9a270-f749-436d-bdff-b39000d8fd77 > ANSIBLE_SSH_KEY
            ae8d79f6-fb4a-434c-94f2-b3860149c5ec > VPS_HOST

      # Load the SSH private key into the ssh-agent
      - name: 🔑 Load SSH Key
        uses: webfactory/ssh-agent@v0.9.1
        with:
          log-public-key: false
          ssh-private-key: ${{ env.ANSIBLE_SSH_KEY }}

      - name: Disable strict host key checking
        run: |
          cat << EOF >> ~/.ssh/config
          Host $VPS_HOST
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
          EOF

      # Run the Ansible playbooks
      - name: 🅰 Deploy VPS
        env:
          BWS_ACCESS_TOKEN: ${{ secrets.BWS_ACCESS_TOKEN }}
          ANSIBLE_CONFIG: ansible/ansible.cfg
        run: |
          ansible-playbook -i ansible/inventory.yml \
           -e "local_repo_path=$GITHUB_WORKSPACE" \
           ansible/playbooks/deploy_vps.yml

      - name: 🅰 Deploy home server
        env:
          BWS_ACCESS_TOKEN: ${{ secrets.BWS_ACCESS_TOKEN }}
          ANSIBLE_CONFIG: ansible/ansible.cfg
        run: |
          ansible-playbook -i ansible/inventory.yml \
           -e "local_repo_path=$GITHUB_WORKSPACE" \
           ansible/playbooks/deploy-home-server-docker.yml